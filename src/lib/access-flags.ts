/**
 * Open-access mode.
 *
 * When true, published courses skip enrollment, premium entitlement,
 * sequential topic unlock, and assessment retake cooldown.
 * Unpublished courses still stay hidden until an admin publishes them.
 *
 * Flip to false to restore enrollment + premium gating on published tracks.
 */
export const OPEN_ACCESS = true;
